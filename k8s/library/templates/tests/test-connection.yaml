apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "labels.fullname" . }}-test-connection"
  labels:
    {{- include "labels.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "labels.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
