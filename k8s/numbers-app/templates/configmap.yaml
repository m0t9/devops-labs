# https://kubernetes.io/docs/concepts/configuration/configmap/
apiVersion: v1
kind: ConfigMap
metadata:
  name: go-app-config
  namespace: default
data:
  config.json: |-
{{ .Files.Get  .Values.config.path | indent 4 }}
