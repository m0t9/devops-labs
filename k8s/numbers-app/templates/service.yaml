apiVersion: v1
kind: Service
metadata:
  name: {{ include "numbers-app.fullname" . }}
  labels:
    {{- include "numbers-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "numbers-app.selectorLabels" . | nindent 4 }}
